# wardrive_bt -- Bluetooth wardrive, logging BLE/BT devices to the SD card
# Target: Marauder v6 / v6.1 (ESP32-WROOM + microSD + Bluetooth), esp32_marauder_v6_1.bin, v1.13.0
# `btwardrive -c` logs every detection (no de-dupe). Files are prefixed `bt_wardrive_` on SD.
# GPS coords require the v6's GPS module with a fix.
# Duration: ~2 min.  SD: required.  GPS: for coords.

stopscan
btwardrive -c
DELAY 120000
stopscan
